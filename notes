# Local run of package
python3 -m vcuui.server

# Local install for development
python3 -m pip install -e .
python3 -m pip uninstall vcu-ui
python3 -m pip uninstall bottle
python3 -m pip uninstall requests


# Distribution to PyDist
# Add PyDist info to .pypirc
# Run on development machine not VCU !
# Don't forget to update _version.py and commit/push to git

python3 setup.py sdist
python3 -m twine upload --repository pydist dist/*


# Install on VCU
# Note: not from prj directory!
cd ~
pip install --index-url https://[tag]:<<access-token>>@index.pydist.com/simple/ vcu-ui

pip install --index-url https://[tag]:<<access-token>>@index.pydist.com/simple/ vcu-ui --upgrade

# the pydist repository can be specified in ~/.config/pip/pip.conf then a normal
# pip install will work.

[global]
index-url = https://[tag]:<<Key>>@index.pydist.com/simple/
